import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent monta el componente raíz y configura el entorno
// tanto para Expo Go como para builds nativos.
registerRootComponent(App);
